export enum PATHS {
    EXPENSES = "driver-expense/",
    ALL_EXPENSES = EXPENSES + "get-all-expenses/", // with bakerRoomId,
    CREATE_EXPENSE = EXPENSES + "create-expense",
    CLOSE_EXPENSE = EXPENSES + "create-cash-box-close"
}